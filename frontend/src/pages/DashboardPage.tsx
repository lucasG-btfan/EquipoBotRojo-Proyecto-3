import { Placeholder } from '../components/layout/Placeholder'

/**
 * Stub de la sección Dashboard (`/dashboard/panel`). El contenido real
 * (estado de contenedores, métricas de Prometheus, últimas alertas, TPW)
 * se implementa en CH14.
 */
export function DashboardPage() {
  return <Placeholder titulo="Dashboard" />
}
