# Tasks: api-logs-injector (CH07)

**Dependencias con otros changes:** CH00 (`cimiento-backend`, completado — provee el stub del router y la app), CH02 (`auth-backend`, completado — provee `usuario_actual`), CH04 (`api-contenedores`, completado — provee `docker==7.*` y los helpers `_crear_cliente`/`_traducir_error` de `docker_service.py`). Ninguna dependencia pendiente: CH07 puede implementarse ya.

**Precondición operativa (no es una tarea de código):** los contenedores `web-server`, `firewall` y `db-server` los crea el operador a mano, en la red del stack y con `util-linux` instalado. El backend los verifica pero nunca los crea (`design.md` §Decisión 2).

## 1. Configuración y contrato

- [x] 1.1 Agregar a `backend/config.py` las variables `SYSLOG_HOST` (default `syslog-ng`) y `SYSLOG_PORT` (default `514`), tipadas en el `Settings` de pydantic-settings.
- [x] 1.2 Documentar las dos variables nuevas en el bloque de entorno del backend de `CLAUDE.md`/`AGENTS.md` y en el `.env.example` si existe (sin valores sensibles).
- [x] 1.3 Crear `backend/schemas/logs.py` con `InyeccionLogsRequest` (campo `categoria: str`), `InyeccionLogsResponse` (campo `mensaje: str`) y `CategoriaLogSchema` (`categoria`, `etiqueta`, `host_origen`, `cantidad_logs`, `emisor_disponible: bool`). Tipado explícito, sin `Any`.
- [x] 1.4 Documentar en `README.md` el procedimiento de preparación de los emisores: los tres `docker run` con `--hostname` y `--network`, más el `apk add --no-cache util-linux` de cada uno. Es precondición del inyector y debe quedar alineado con lo escrito en la tesis.

## 2. Servicio de inyección

- [x] 2.1 Crear `backend/services/logs_injector_service.py` con el docstring de módulo, la constante `TIMEOUT_INYECCION = 60.0` y las excepciones propias `ErrorInyeccionLogs` y `CategoriaInvalida`.
- [x] 2.2 Definir la constante `CATALOGO_CATEGORIAS` con las 9 categorías individuales según la tabla de `design.md` §Decisión 3: por cada una, `etiqueta` en español, `host_origen` (`web-server` / `firewall` / `db-server`), y lista de entradas `(tag, prioridad, mensaje, repeticiones)`. Mensajes tomados de `docs/SDD.md` §3.3. Definir también `EMISORES = ("web-server", "firewall", "db-server")`.
- [x] 2.3 Implementar `listar_categorias()` — devuelve el catálogo aplanado para el frontend, incluyendo `paquete_completo` con la suma total de logs calculada del propio diccionario (sin duplicar datos), y la disponibilidad del emisor de cada categoría (2.7). Si Docker no responde, el catálogo se devuelve igual con `emisor_disponible = false`, no con error.
- [x] 2.4 Implementar `_construir_comando(entrada)` — arma la lista de argumentos `["logger", "-n", SYSLOG_HOST, "-P", str(SYSLOG_PORT), "-d", "-t", tag, "-p", prioridad, "--rfc3164", mensaje]`. Nunca un string, nunca un shell.
- [x] 2.5 Implementar `_resolver_entradas(categoria)` — devuelve la lista de entradas de log a emitir; levanta `CategoriaInvalida` si la clave no está en el catálogo; expande `paquete_completo` como la concatenación ordenada de las 9 individuales.
- [x] 2.6 Implementar `_resolver_redes_syslog(cliente)` — inspecciona el contenedor `SYSLOG_HOST` y devuelve los nombres reales de sus redes. No hardcodear `security-network`: Compose la materializa como `<proyecto>_security-network`.
- [x] 2.7 Implementar `_verificar_emisor_sync(cliente, nombre, redes)` — comprueba en orden: (a) el contenedor existe, (b) su estado es `running`, (c) comparte al menos una red con `syslog-ng`, (d) tiene `logger` de util-linux (`exec_run(["sh","-c","command -v logger"])`). Devuelve el contenedor si todo pasa; si no, levanta `ErrorInyeccionLogs` con el mensaje accionable de la causa concreta (`design.md` §Decisión 7 y riesgos R6/R7): comando `docker run` para (a)/(b), `docker network connect` para (c), `apk add --no-cache util-linux` para (d). Nunca crea, arranca ni modifica un contenedor.
- [x] 2.8 Implementar `_inyectar_sync(entradas)` — crea el cliente Docker reutilizando `_crear_cliente()` de `docker_service.py`, resuelve las redes (2.6), verifica **todos** los emisores involucrados **antes** de emitir el primer log y los cachea, ejecuta un `exec_run` por log en orden secuencial, cierra el cliente en `finally`, y devuelve la cantidad emitida. Si un `exec_run` devuelve `exit_code != 0`, aborta e informa cuántos alcanzaron a emitirse.
- [x] 2.9 Implementar `inyectar_categoria(categoria) -> int` — envuelve `_inyectar_sync` en un único `asyncio.to_thread` bajo `asyncio.wait_for(TIMEOUT_INYECCION)`, y traduce las excepciones de docker-py a `ErrorInyeccionLogs` con mensaje en español reutilizando `_traducir_error()` de `docker_service.py`.

## 3. Router

- [x] 3.1 Reemplazar el stub de `POST /api/logs/inject` en `backend/routers/logs.py`: recibe `InyeccionLogsRequest`, declara `response_model=InyeccionLogsResponse`, conserva `Depends(usuario_actual)` y devuelve `{"mensaje": "X logs inyectados para categoría Y"}`.
- [x] 3.2 Manejo de errores explícito del endpoint: `CategoriaInvalida` → `400` con mensaje en español que enumere las categorías válidas; `ErrorInyeccionLogs` → `503` con el mensaje accionable del servicio; `except Exception` final → `500` con mensaje en español.
- [x] 3.3 Agregar `GET /api/logs/inject/categorias` con `response_model=list[CategoriaLogSchema]`, protegido con `Depends(usuario_actual)` y con el mismo bloque de manejo de errores.
- [x] 3.4 Verificar que el router siga registrado en `backend/main.py` y que ambas rutas aparezcan en `/docs` con sus schemas.

## 4. Verificación manual

- [x] 4.1 **Sin los emisores creados**, probar `POST /api/logs/inject` con `{"categoria": "root_login"}` → `503` cuyo mensaje nombra `web-server` e incluye el `docker run` literal. Confirmar que el backend no creó ningún contenedor. **Verificado real:** `503`, mensaje con `docker run -d --name web-server --hostname web-server --network <red> alpine sleep infinity`; `docker ps -a` confirmó que no se creó ningún contenedor.
- [x] 4.2 Crear los emisores siguiendo el procedimiento documentado en 1.4 y repetir: `200` con el mensaje de cantidad y categoría. **Verificado real:** `200 {"mensaje":"2 logs inyectados para categoría root_login"}`.
- [~] 4.3 Confirmar que los logs emitidos aparecen en `GET /api/alerts/log` (CH06) y/o llegan a `syslog-ng`, y que la alerta resultante en PostgreSQL tiene `source_host = web-server`. **Parcialmente verificado:** los logs llegaron a `syslog-ng` y aparecieron en `/var/log/security/alerts.log` con IPs de origen distintas por emisor (`172.19.0.3/.4/.5` para web-server/firewall/db-server). La columna `source_host` en PostgreSQL **no se pudo verificar** en este entorno: solo se levantó `syslog-ng` (no `security-postgres` ni `n8n`, que son los que persisten la alerta).
- [x] 4.4 Probar `{"categoria": "no_existe"}` → `400` con la lista de categorías válidas, y verificar que no se emitió ningún log. **Verificado real:** `400` con las 10 categorías listadas.
- [x] 4.5 Probar `paquete_completo` → `200` con la cantidad total (35), dentro del timeout, y verificar que las alertas llegan con `source_host` distinto según la categoría. **Verificado real:** `200 {"mensaje":"35 logs inyectados para categoría paquete_completo"}` en 2.9s (timeout 60s); `alerts.log` mostró las tres IPs de origen distintas.
- [ ] 4.6 Verificar específicamente `service_restart` y `log_legitimo` contra el decoder de Wazuh (`tag`/prioridad ya confirmados por el usuario con ejemplo literal — riesgo R4 resuelto en `design.md`) y ajustar si no se comportan como se espera. **No verificable en este entorno:** Wazuh no estaba levantado (solo `syslog-ng`). Señal indirecta observada: `log_legitimo` no generó entrada en `alerts.log` (filtro de syslog-ng, consistente con ser tráfico benigno) y `service_restart` se emitió sin error de `exec_run`.
- [x] 4.7 Verificar el riesgo R6: crear `web-server` **sin** el `apk add util-linux`, inyectar, y confirmar que el `503` distingue esta causa e indica el comando de instalación, sin modificar el contenedor. **Verificado real — encontró y corrigió un bug de diseño:** el chequeo original de 2.7 (`command -v logger`) no detecta el caso real, porque la imagen `alpine` trae preinstalado el `logger` de BusyBox (sin soporte `--rfc3164`); `command -v logger` da `exit_code 0` igual. Se reprodujo con un contenedor `firewall` sin `apk add`: el primer intento devolvió `503` genérico de fallo en emisión, no el mensaje accionable esperado. Se corrigió `_verificar_emisor_sync` en `backend/services/logs_injector_service.py` para comprobar `logger --help | grep -- --rfc3164` en vez de `command -v logger`; repetido el test, ahora responde `503` con el mensaje correcto y el comando `apk add --no-cache util-linux`.
- [x] 4.8 Verificar el riesgo R7: crear `firewall` sin `--network`, inyectar `port_scan`, y confirmar que responde `503` con el `docker network connect` en vez de emitir logs que se pierden. **Verificado real:** `503` con `docker network connect equipobotrojo-proyecto-3_security-network firewall`.
- [x] 4.9 Detener `db-server` y confirmar que `sudo_usage` responde `503` señalando que está detenido. **Verificado real:** `503` "existe pero no está en ejecución (estado: exited)".
- [ ] 4.10 Con Docker Desktop detenido, confirmar que ambos endpoints responden `503` con mensaje en español y no `500`, y que el catálogo sigue respondiendo con `emisor_disponible = false`. **No verificable en este entorno:** detener Docker Desktop por completo habría interrumpido el resto de la sesión de verificación y las herramientas de este agente bloquean comandos Docker destructivos/de apagado. El camino de código es el mismo `_traducir_error()`/`ErrorDocker` ya validado en CH04 (`docker_service.py`), reutilizado sin cambios por `_crear_cliente()` y `_emisores_disponibles_sync()`.
- [x] 4.11 Confirmar que ambas rutas rechazan la petición sin token JWT. **Verificado real:** `401` en `POST /api/logs/inject` y en `GET /api/logs/inject/categorias` sin header `Authorization`.
- [x] 4.12 Con solo `web-server` y `firewall` creados, inyectar `paquete_completo` y confirmar que falla con `503` **sin emitir ningún log parcial**. **Verificado real:** con `db-server` detenido, `503` "no está en ejecución"; `wc -l /var/log/security/alerts.log` idéntico antes y después (2 líneas), confirmando cero emisión parcial.
