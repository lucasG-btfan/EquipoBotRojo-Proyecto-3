## 1. Tipos TypeScript

- [x] 1.1 En `frontend/src/types/tickets.ts`: eliminar el union `EstadoTicket` (verificar antes con grep sobre `frontend/src` que no tiene consumidores) y cambiar en la interfaz `Ticket` los campos a `status: string` y `priority: string | null`. Actualizar el JSDoc del archivo para reflejar el hallazgo real: `security_tickets.status` y `.priority` son `VARCHAR` libre sin enum ni check constraint, y los valores producidos por los workflows de n8n son `open` / `urgent` / `resolved` y `low` / `medium` / `critical` (ver design.md D2 y el bloque "Descubrimiento clave" del Context). El resto de los 13 campos no cambia.
- [x] 1.2 En el mismo archivo, agregar las constantes de valores conocidos con `as const` y los tipos derivados de ellas: `ESTADOS_TICKET_CONOCIDOS = ['open', 'urgent', 'resolved']` con `EstadoTicketConocido = (typeof ESTADOS_TICKET_CONOCIDOS)[number]`, y `PRIORIDADES_TICKET_CONOCIDAS = ['low', 'medium', 'critical']` con `PrioridadTicketConocida`. Definir también el union del filtro de la UI: `FiltroEstadoTicket = EstadoTicketConocido | 'todos'`. Prohibido `any`.

## 2. Servicio de acceso a la API

- [x] 2.1 Crear `frontend/src/services/ticketsService.ts` usando `apiClient`, con la función `obtenerTickets({ limit, offset, estado? })` → `GET /api/tickets`, tipada como `Promise<RespuestaPaginada<Ticket>>` (design.md D3). El parámetro `estado` `undefined` no debe enviarse: pasarlo dentro de `params` y dejar que axios lo omita. Sin `try/catch` propio: el interceptor de `apiClient` ya normaliza el error a `Error` con mensaje en español. JSDoc de encabezado referenciando `backend/routers/tickets.py`.
- [x] 2.2 Exportar en el mismo `ticketsService.ts` la constante `FILAS_POR_PAGINA = 20`, enviada siempre como `limit` explícito (no confiar en el default 15 del backend). No hardcodear el número en el componente.

## 3. Sub-componente DetalleTicket

- [x] 3.1 Crear `frontend/src/components/tickets/DetalleTicket.tsx`: componente de presentación puro, sin estado ni llamadas HTTP. Props: `{ ticket: Ticket }`. Renderiza un panel con encabezado que incluye el `ticket_number` (para que quede claro a qué fila corresponde, design.md D5) y los campos que no entran en la tabla: descripción completa (`description`), responsable (`assigned_to`), última actualización (`updated_at` formateado) y referencia de alerta (`alert_reference`).
- [x] 3.2 Resolver los campos vacíos con un texto explícito en español, no con `null` ni cadena vacía: descripción ausente → "Sin descripción."; responsable ausente → "Sin asignar"; referencia de alerta ausente → "Sin alerta asociada". Estilo del panel coherente con el resto del panel (`rounded-lg border border-borde`, fondo y tipografía como los bloques existentes de `gestion-ips`).

## 4. Sub-componente TablaTickets

- [x] 4.1 Crear `frontend/src/components/tickets/TablaTickets.tsx` con el estado base: `respuesta: RespuestaPaginada<Ticket> | null`, `cargando`, `error`, `offset`, `filtroEstado: FiltroEstadoTicket` (inicial `'todos'`) y `ticketExpandido: number | null` (inicial `null`). Un `useEffect` con dependencias `[offset, filtroEstado]` llama a `obtenerTickets`, enviando `estado: undefined` cuando el filtro es `'todos'` (design.md D4). Sin `usePolling` y sin agregar constantes a `constants/polling.ts`.
- [x] 4.2 Implementar en el mismo archivo el helper exportado `formatearFecha(valor: string | null): string` según design.md D7: `null` o cadena vacía → `'—'`; normalizar el separador con `valor.replace(' ', 'T')` antes de `new Date(...)` porque el backend emite `"2026-08-19 14:32:11.123456"` y no ISO 8601; si `Number.isNaN(fecha.getTime())` devolver el valor crudo en lugar de `"Invalid Date"`; si es válida devolver `fecha.toLocaleString('es-AR')`.
- [x] 4.3 Implementar los dos mapas de presentación de design.md D6, tipados como `Record<string, VarianteBadge>` y `Record<string, string>` (clave `string`, no el union cerrado, para que una clave desconocida sea legal): estado `open` → "Abierto"/`info`, `urgent` → "Urgente"/`peligro`, `resolved` → "Resuelto"/`exito`; prioridad `critical` → "Crítica"/`peligro`, `medium` → "Media"/`advertencia`, `low` → "Baja"/`info`. Reserva con `??`: variante `neutro` y el valor crudo como etiqueta. `priority` en `null` se muestra como `—` en texto, **sin** renderizar `Badge` (su prop `children` es `string`, design.md D5).
- [x] 4.4 Renderizar la barra superior del bloque: título "Tickets de seguridad", `<select>` de filtro por estado con la opción "Todos" más una opción por cada valor de `ESTADOS_TICKET_CONOCIDOS` usando las etiquetas en español del mapa del punto 4.3, y botón "Actualizar" que re-dispara la consulta con el estado actual. Cambiar el filtro debe setear `offset` a 0 **y** `ticketExpandido` a `null` en la misma actualización (design.md D4).
- [x] 4.5 Renderizar la tabla con `components/common/Tabla`, columnas: N.º de ticket (`ticket_number`, `font-mono`), Título (`title`, `—` si es null), Estado (`Badge` según el mapa), Prioridad (`Badge` o `—`), Categoría (`category`, `—` si es null), IP de origen (`source_ip`, `font-mono`, `—` si es null), Puntaje (`threat_score`, `—` si es null), Creado (`created_at` con `formatearFecha`) y Detalle. `claveFila` = `fila.id`. `onCambiarOffset` actualiza el estado `offset`. No reordenar los items en el cliente: el backend ya los entrega por `created_at` descendente.
- [x] 4.6 Implementar la columna Detalle: un botón por fila que alterna `ticketExpandido` entre el `id` de esa fila y `null` (solo un detalle abierto a la vez). El botón de la fila abierta debe verse en estado activo para que sea evidente a qué ticket corresponde el panel. Renderizar `<DetalleTicket ticket={...} />` inmediatamente debajo del componente `<Tabla>` cuando `ticketExpandido` no es `null` y el ticket sigue presente en `respuesta.items`. No modificar `components/common/Tabla` (design.md D5).
- [x] 4.7 Renderizar el estado de error de carga sobre la tabla con el estilo `rounded-lg border border-peligro/30 bg-peligro/10 p-4 text-sm text-peligro` (mismo patrón que `TablaIpsBloqueadas`), dejando disponible el botón "Actualizar" para reintentar sin recargar la página. Mostrar, fuera de la tabla y en la barra de filtros, el mensaje "No hay tickets para el filtro aplicado." cuando `total === 0` y el filtro no es `'todos'`; con el filtro en `'todos'` alcanza el mensaje vacío genérico que ya provee `Tabla`.

## 5. Página orquestadora

- [x] 5.1 Crear `frontend/src/pages/TicketsPage.tsx`: título `h1` "Tickets" y el bloque `TablaTickets` dentro de una `<section>`, con la estructura visual de `GestionIpsPage` (`flex flex-col gap-6 p-6 lg:p-8`). La página solo compone: nada de estado ni de llamadas HTTP en este archivo (design.md D1).

## 6. Routing

- [x] 6.1 En `frontend/src/App.tsx`: agregar `import { TicketsPage } from './pages/TicketsPage'` y reemplazar `<Placeholder titulo="Tickets" />` por `<TicketsPage />` en la ruta `path="tickets"`. No tocar el resto del árbol de rutas, el sidebar (ya apunta a `/dashboard/tickets`) ni `ProtectedRoute` (design.md D9).

## 7. Verificación

- [x] 7.1 Correr `npm run build` en `frontend/` (`tsc -b && vite build`) y confirmar que compila sin errores de tipos; correr `npm run lint` y dejarlo limpio. Verificar que no quedó ningún `any` ni ninguna URL/IP hardcodeada en los archivos creados, y que la eliminación de `EstadoTicket` no rompió ningún import.
- [x] 7.2 Verificación manual con el stack levantado: navegar a `/dashboard/tickets` y comprobar el listado cargando, el orden de más reciente a más antiguo, la paginación con el indicador de rango, el filtro por cada estado y por cada prioridad (y su combinación) con el reinicio a la primera página al filtrar, el botón "Actualizar", la apertura y cierre del detalle, y el render de un ticket con campos nulos (título, prioridad, categoría, descripción). Verificar también el caso de error cortando el backend: debe verse el mensaje en español y poder reintentarse con "Actualizar".

## 8. Filtro por prioridad (ajuste post-implementación)

- [x] 8.1 Backend (`backend/routers/tickets.py`): agregar query param opcional `prioridad` (mismo patrón español/inglés que `estado`→`status`) que filtra `Ticket.priority` con match exacto, aplicado tanto a la consulta paginada como al conteo total. Mismo manejo de errores existente (try/except con detalle en español).
- [x] 8.2 Frontend — `types/tickets.ts`: agregar `FiltroPrioridadTicket = PrioridadTicketConocida | 'todas'`, análogo a `FiltroEstadoTicket`.
- [x] 8.3 Frontend — `services/ticketsService.ts`: agregar `prioridad?: string` a los parámetros de `obtenerTickets`, con la misma semántica de omisión en `undefined` que ya tiene `estado`.
- [x] 8.4 Frontend — `components/tickets/TablaTickets.tsx`: agregar estado `filtroPrioridad: FiltroPrioridadTicket` (inicial `'todas'`), incluirlo en las dependencias del `useEffect` de consulta, y un `cambiarFiltroPrioridad` que resetea `offset` a 0 y cierra el detalle abierto (mismo criterio que `cambiarFiltroEstado`, renombrado desde `cambiarFiltro`). Agregar el `<select>` de prioridad (Todas + `PRIORIDADES_TICKET_CONOCIDAS`) y etiquetas visibles "Estado" y "Prioridad" arriba de cada `<select>` respectivo.
